[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "mazegen"
version = "1.0.0"
description = "A 42 curriculum maze generator package."
authors = [
    { name = "dtporhomo" },
    { name = "team_mate2" }
]
requires-python = ">=3.10"